class Darasa():
    """attributes:
            name: A string representing the student's name
            members: A list showing the  members belonging to the class darasa  """

    def __init__(name , members):
        name.members = [ ]


    @property
    def addclassmember(name):
            """Adds new members in the class list"""
            if Rerimoi not in name.members:
                    name.members.append('Rerimoi')
                    name.members.append('Sandra')
                    name.members.append('Linda')
                    return "Rerimoi has been added to the new list."
    def removeclassmember(name):
            """Adds name in the list"""
            if Rerimoi not in members:
                members.remove('Sandra')

                return members

